import { bootstrap } from '@angular/platform-browser-dynamic';
import {AppComponent} from './app';

bootstrap(AppComponent);    // No directives needed

// var Datastore = require('nedb')
//   , db = new Datastore();
